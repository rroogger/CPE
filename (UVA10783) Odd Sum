#include <iostream>

using namespace std;

int main(){
int n;

	while(cin>>n){
		int a,b;
		int c=1;
		while(cin>>a>>b){
			
			int count=0;
			for(int i=a;i<=b;i++)
			{
			if(i%2==1)
			count+=i;
			}
		cout<<"Case "<<c<<": "<<count<<endl;
		c++;
		}
	}
return 0;
}

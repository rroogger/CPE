#include <iostream>

using namespace std;

int main(){
int min,max;
while(cin>>min>>max){
	if(min==0&&max==0){
	break;
	}
			int count=0;
			for(int i=min;i<=max;i++){
				int c=sqrt(i);
				
				if(c*c==i){
				count++;
				}
		}
		cout<<count<<endl;	
}

return 0;
}
